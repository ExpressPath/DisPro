{
  "name": "dispro",
  "version": "0.1.0",
  "description": "Dispro distributed compute order and task planning foundation.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test tests/order-orchestrator.test.ts tests/api-server.test.ts",
    "server": "tsx src/server.ts",
    "simulate": "tsx src/cli/simulate-order.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
